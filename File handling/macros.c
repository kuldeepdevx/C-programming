#include <stdio.h>
#include <string.h>
#define PI 3.14159265
#define k "kuldeep"
#define Area (PI*r*r)
int main(){
    // double x=PI;
    // printf("%i",x);

    // char name[]=k;
    // printf("%.6f",PI);
    int r;
    printf("Enter radius:");
    scanf("%d",&r); 
    printf("ans is: %f",Area);
    return 0;
}